/** The AI companion's display name. Change here to rename everywhere. */
export const COMPANION_NAME = 'Echo';

// TODO: wire COMPANION_NAME into any screen that displays the
// companion's name as text (e.g. "Echo is thinking…" loading states,
// future chat headers). Currently only used as a visual SVG asset.
