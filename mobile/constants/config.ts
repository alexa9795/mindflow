/** The app's public/product name (App Store, paywall, store-facing copy). */
export const APP_NAME = 'MindFlow';

/** The AI companion's display name. Unified with the app brand (MindFlow). */
export const COMPANION_NAME = 'MindFlow';

/** Hosted legal documents (GitHub Pages at mindflowjournal.app). */
export const PRIVACY_POLICY_URL = 'https://mindflowjournal.app/privacy.html';
export const TERMS_OF_SERVICE_URL = 'https://mindflowjournal.app/terms.html';

// Note: the AI companion shares the app name (MindFlow) — there is no
// separate persona name. COMPANION_NAME is kept as a single source of truth
// for copy that refers to the AI feature (e.g. the consent modal).
